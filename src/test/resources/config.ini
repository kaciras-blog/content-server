# 网站安全设置
web.port=2375
web.SessionCookieDomain=localhost
web.CorsOrigin=*
# 是否校验CSRF令牌
web.csrfVerify=true
web.accessLog=false
# SSL证书，不配置的话将使用http
# web.cert = /etc/letsencrypt/archive/api.kaciras.net/cert1.pem
# web.pvk = /etc/letsencrypt/archive/api.kaciras.net/privkey1.pem
# web.chain = /etc/letsencrypt/archive/api.kaciras.net/chain1.pem
# 数据库配置
db.driver=org.mariadb.jdbc.Driver
db.user=root
db.password=IY*J<0+T.[Skcoxd
db.url=jdbc:mariadb://localhost:3316/BlogTest?allowMultiQueries=true
# 发布文章后删除草稿
draft.deleteAfterSubmit=false
# 将文章的关键词，简介放入HTML页面头的Meta中
post.keywordMeta=true
post.descriptionMeta=true
# 允许匿名评论
discussion.allowAnonymous=true
# 是否禁止评论
discussion.disable=false
# 评论审核模式
discussion.filter=NO_FILTE
# 设置为true时，任何访问者都视为超级用户，以便于调试，生产环境请设为false
permission.debugMode=false
# 是否启用角色继承，默认为true，即启用继承
# false禁止继承后，角色所包含的角色的权限将不算作角色的权限
permission.checkRoleIncludes=true

test.config.binding.v0=123456
test.config.binding.enum = FIRST


