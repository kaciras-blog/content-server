# 测试配置，其中web部分与线上一致、数据库与开发环境一致
spring:

  main:
    allow-bean-definition-overriding: true

kaciras:

  session:
    cookie:
      domain: test.example.com

  cors:
    allowed-origins: https://test.example.com
