spring:

  main:
    allow-bean-definition-overriding: true
    log-startup-info: false

app:

  session:
    cookie:
      domain: test.example.com

  cors:
    allowed-origins: https://test.example.com
