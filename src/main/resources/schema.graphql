schema {
	query: Query
}

type Query {
	article(id: ID!): Article
	articles(start: Int, count: Int): [Article!]!
}

type Article {
	id: ID!
	title: String!
	urlTitle: String!
	create: String!
	cover: String!
}

type Category {
	id: ID!
	name: String!

}
